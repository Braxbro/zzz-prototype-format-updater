data:extend{
    {
        -- Disables this mod's fixing functionality without preventing it from printing fixes it finds.
        type = "bool-setting",
        name = "pfu-debug-mode",
        default_value = false,
        setting_type = "startup"
    }
}